# Local Asset Studio

Personal Windows creative sandbox; proposed T1. Read CURRENT_STATE.md before changing runtime setup.
Keep one writer in this checkout and preserve user experiments. Models and installed applications live outside Git.
Run `python -m unittest discover -s tests` and `python scripts/validate-repo.py` for app/catalog changes.
Use `scripts/Start-Studio.ps1` for the local launcher. Never auto-submit generation on page load.
Keep the server loopback-only. Preserve full recipes and known prompt IDs; never repeat an uncertain submission.
Treat successful generation separately from art acceptance and model licensing.
HUMAN_TODO.md records subjective choices; do not check them off without user input.
