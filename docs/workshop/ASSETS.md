# Asset inventory and provenance

No external stock site, image API, web font, character franchise, model download or remote runtime asset was added.

| Asset | Use | Provenance / scope |
| --- | --- | --- |
| `app/static/workshop-assets/arcade-horizon.svg` | Decorative Arcade heading motif | Existing original geometric SVG; no embedded image/script/font |
| `app/static/workshop-assets/sakura-branch.svg` | Decorative Sakura heading motif | Existing original botanical SVG; no embedded image/script/font |
| `app/static/workshop-assets/night-shift.svg` | Reviewable source for Night Shift ambience | Original same-camera geometric rainy-city/workbench composition authored for this change |
| `app/static/workshop-assets/quiet-morning.svg` | Reviewable source for Quiet Morning ambience | Original daylight counterpart using the same geometry and camera |
| `app/static/workshop-immersive-core.css` | Production renditions | Contains inert base64 data copies of the two ambience SVGs because the application static handler does not serve SVG files directly |
| `examples/workflow-lab/anima.png` | Standalone prototype illustration | Existing repository example; not generated by this redesign |
| `examples/gallery/pixel-lora-128.png` | Standalone prototype pixel-art alternative | Existing repository example; unchanged source bytes |

The ambience art is decorative and has empty semantics. It carries no label, instruction, progress, readiness claim or button. It does not cover prompt fields, controls or errors. Forced-colour mode removes it, and Ambience None reserves no empty region.

Every SVG source contains only ordinary vector shapes, gradients and filters. There are no scripts, `foreignObject`, external references, fonts, remote URLs or executable configuration. Production does not insert prototype artwork into the real result gallery. The real gallery continues to show the user's outputs and provenance.

Existing examples retain their existing provenance and terms. This document does not relicense model outputs, certify character rights, treat a passing frontend test as artistic acceptance or claim that the visual environment was produced by an installed Studio model.

`app/static/workshop-immersive.css` remains the stable entrypoint and imports the core before applying narrow responsive overrides. Use normal platform fonts. Future skins/environments must remain allow-listed presentation data, not generation defaults or backend state. Run the full presentation-state and viewport contracts for each addition.
