# Asset inventory and provenance

No external stock site, image API, web font, character franchise, model download or remote runtime asset was added.

| Asset | Use | Provenance / scope |
| --- | --- | --- |
| `app/static/workshop-assets/arcade-horizon.svg` | Decorative Arcade heading motif | Original geometric SVG authored for this change; no embedded image/script/font |
| `app/static/workshop-assets/sakura-branch.svg` | Decorative Sakura heading motif | Original botanical SVG authored for this change; no embedded image/script/font |
| `examples/workflow-lab/anima.png` | Standalone prototype illustration | Existing repository example at design baseline `b8b1409`; not generated by this redesign |
| `examples/gallery/pixel-lora-128.png` | Standalone prototype pixel-art alternative | Existing repository example at the same baseline; unchanged source bytes |

The two new SVGs are decorative CSS backgrounds. They do not cover prompt fields, controls or readiness messages, and disappear on narrow screens and in forced-colour mode. Production never inserts the prototype artwork into the real result gallery. The real gallery continues to show the user's real outputs and provenance.

Existing examples retain their existing provenance and terms. This document does not relicense model outputs, certify a character's rights, or treat a passing frontend test as artistic acceptance. Sakura is an anime-inspired, non-explicit palette; it is not an erotic-content workflow or model policy.

Use normal platform fonts. Future skin additions should add only allow-listed token sets and optional local decorative files, not executable configuration, third-party embeds or generation defaults. Run the full layout × skin browser matrix for each addition.
