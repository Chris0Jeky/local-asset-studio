# Source reconciliation and scope of this delivery

Inspected base: `be14fa4104890401360cbb707d48132bd64c688e`, tree `d5e39a94fa9dd92a20e74ee5042571255d5e32e4`. This is a dated source snapshot, not a claim that main will remain at that revision. Research/design date: 14 September 2026. Reinspect live state before implementation.

## Foundations actually present

`AGENTS.md`, `CLAUDE.md`, current authority, active pointer, CURRENT_STATE and HUMAN_TODO were read. The repository is a one-user loopback Python/plain-JS Studio over external ComfyUI/native installations. Preset-to-graph binding and existing Production ownership are preserved.

- `character_media.py` already provides bounded, exact rectangular extraction. It retains backgrounds/neighbor fragments. Its demonstration compositor requires 13 panels; it is not a generic sheet builder.
- Existing character-edit contracts and pixel bridge distinguish context, write and protection. Their RGBA/tRNS reconstruction checks and no-resample crop/padding are valuable foundations, not anatomy evidence.
- `character_edit_bridge.py` and its plan/IO companions already run one-actor Qwen edits through existing Stage/Start/Collect/Compose operations. Current neural support rejects genuinely transparent context and colour ambiguity, and does not pass a grayscale write mask into Qwen. Some older broad architecture paragraphs predate these implementations.
- Merged #226 provides captured unsaved Krita document revisions and reversible proposed layers. Its native synthetic proof is not general GUI/layer compatibility or neural quality acceptance. #225's dependency-manifest gap remains separately owned.
- Merged #240 provides explicitly registered campaign allowances shared across edit revisions. Global per-case repair-slot allocation and real accepted repair campaigns remain outstanding; do not build another root budget service.
- #224 already addressed mixed known/unknown recovery. Existing schema-validator bugs were fixed in #156; do not repeat those implementations under this programme.

## Concurrent work at inspection

PRs #233–#239 were open at the inspected snapshot: filtered library context; bounded reference byte provenance (#234/#229); no-clobber Workspace publication (#235/#230); complete HTTP reply framing (#236/#231); source-aware recipe advice (#237); backend schema capture (#238); and mixed-batch owner binding (#239/#228). These are integration dependencies or neighboring responsibilities, not work delivered by this PR. Subsequent merge status belongs to live GitHub.

#232 owns reviewed ordered multi-reference apply; advice alone does not attach or execute a source. #66 already owns critic calibration and #72 the frozen scoped-edit benchmark. #118/#120/#123 own guided/workflow-document/agent foundations; #178 owns stage-aware resource admission. The new issues narrow repair-specific gaps instead of duplicating those owners.

## New programme issues

#243 epic; #244 normalized intake/panels; #245 transforms/effective masks; #246 isolation/matting/visibility; #248 native candidate adapters/geometry; #249 instances/contacts; #250 bounded strategy policy; #251 shared guided repair; #256 finishing/recomposition; #257 qualification. Gaps in numbering are concurrent repository activity, not missing programme tasks.

## Hardware and owner decisions

Historical logs describe a 16 GiB Radeon and older 32 GiB system-RAM configurations. Later user context reports 64 GiB physical RAM. These are not a current measurement; reobserve physical RAM, Windows commit headroom, VRAM, runtime and model footprint before any execution. A configured 64 GiB page file is a different fact from physical RAM and does not prove that a previous crash is resolved.

HUMAN_TODO's previous creative choices and q-1 through q-4 are answered. They were not reopened or changed. Future repair/production-art acceptance and model/asset usage decisions remain distinct from those recorded choices. The historical pilot allowance is not renewed by this architecture task.

## Verification scope

The container could not clone the repository because GitHub DNS was unavailable. Source reconciliation and publication use the connected GitHub tool. Local focused testing uses the new standalone stdlib scaffold and its tests; it is not described as a local full-repository run. Full hosted suite/validator results are recorded on the actual PR when observed.

No user image was cropped, modified, published or used for generation in this slice. Examples contain synthetic identifiers and declared hashes, not real approved canon or image evidence. No model/node installation, native application execution, runtime restart, campaign registration, generation, repository visibility change or main merge is part of this delivery.
