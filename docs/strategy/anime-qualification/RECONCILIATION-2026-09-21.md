# Research-to-repository reconciliation — 21 September 2026

Owner: #552 under #313/#14. The research remains frozen at **2026-09-15**;
this is a later implementation review, not a backdated replacement report.

## Evidence and what was already delivered

Inspected live `main`: `f16ed8ea2fbb23e9bf64b726745790de5909577f`, tree
`822f32757909f10ad0fe49543f33b4e76d16c4bd`. The owner-supplied main ZIP carries
that commit in its archive comment; SHA-256:
`9a7182bf3b3685e8919060061aef0125bf26194dc844fe0a8963cc5c621d5471`.
The reconstructed local Git index matches the upstream tree exactly, including the
tracked CRLF fixture. This proves source identity, not installed-workstation state.

The 26-page report was recovered through Library retrieval, including rendered
page tables where available. Its raw-byte materialization was unavailable. The PDF
hash in [SOURCES.md](SOURCES.md) is therefore **inherited intake evidence**, not a
hash recomputed in this pass. Do not publish the report or private reference art.

[PR #569](https://github.com/Chris0Jeky/local-asset-studio/pull/569) already merged
the 54-entry page-addressed decomposition, seven integrated documents and the
source-scope safeguard. [PR #572](https://github.com/Chris0Jeky/local-asset-studio/pull/572)
already merged exact authored-template profiles for Aesthetic 1.1, Base 1.0 and
Animagine 4.0 Opt. These are existing product capabilities, not new accomplishments
of this pass. A template match still does not authenticate upstream advice or
attest loaded weights.

The source snapshot also contains the adopted adult-illustration/taxonomy stack.
HUMAN_TODO q-29's **programme adoption** was answered on 20 September; its remaining
content-envelope questions and q-31 remain open. Replace the stale “parked stack”
assumption, without importing that programme's content scope into the non-sexual
fantasy pack or changing any human decision. Existing Combine/Klein, pose-then-face,
WAI repair and Anima Base records remain comparison baselines.

## Ten deliverables: reuse, change and remaining gate

| Requested deliverable | Existing owner/evidence | This continuation and remaining work |
| --- | --- | --- |
| 1. Source ledger | [SOURCES.md](SOURCES.md), model library and source-guidance records | Adds a dated [machine-readable review ledger](source-review-2026-09-21.json). Distinguishes immutable card access, mutable observations, inherited pins and failed retrieval. Remaining exact source captures stay unresolved. |
| 2. Model/version matrix | `models/library.json`, catalog/API graphs, Prompt Lab profiles | New derived route inspector joins selected declarations, settings, hashes and profiles. Actual prediction type/precision, loaded weights and node revisions are not inferred from filenames. No second model registry. |
| 3. Civitai/LoRA matrix | Canonical library, retained provider/source evidence, #144 and #765 | Inspector retains exact selected component declarations, including all conflicting candidates. It does not verify current Civitai flags or install files. Active source-evidence and installer work remains with its existing PRs. |
| 4. Windows/Linux runtime | `app/job_resources.py`, host-memory/admission owners #302/#303/#307 | Adds explicit unobserved cold/warm/switch/VRAM/RSS/commit/cleanup fields to inspection output, not fabricated results. Actual paired environments and measurement remain operator work. |
| 5. Exact prompt profiles | #572, `research/prompt-studio/profiles.json` | Reports exact positive/negative template matches versus legacy recipe associations. Standalone Turbo and reference-aware Qwen Q4/Lightning versus native BF16 profiles still need dedicated tests. |
| 6. Multi-reference capability | Reference Intelligence, catalog slots, Workflow Studio #21/#232 | Preserves catalog role order and crop policy while leaving native capacity, leakage and drift unknown. Three authored slots do not prove a native maximum or three-role fidelity. |
| 7. Finite benchmarks | [QUALIFICATION.md](QUALIFICATION.md), Experiment Lab, 48-cell pose plan | Keep deterministic task/cell identities, retained failures and physical-attempt caps in the existing campaign owner. Do not execute the paper's illustrative JSON or silently expand the 48-cell plan. |
| 8. Separate result domains | Workspace/reviews and existing experiment artifacts | Inspector emits separate hard-constraint, visual-quality, acceptance, effort and resource sections, all unobserved. Only retained runs and independent/owner reviews may populate measured outcomes. |
| 9. Architecture delta | [ARCHITECTURE.md](ARCHITECTURE.md) and existing services | One offline projection module reusing guidance and graph provenance; one CLI; no service, queue, database, runtime import or new install authority. |
| 10. Route dispositions | [PORTFOLIO.md](PORTFOLIO.md), owner baseline decisions | Qualification priority is not promotion. Preserve private comparators; do not replace measured routes or declare a new artistic winner from source cards or unit tests. |

## Concrete software delivered

[ROUTE-INSPECTION.md](ROUTE-INSPECTION.md) describes the callable/CLI contract.
`studio_workflow.qualification` derives a fresh report from the **existing** catalog,
API graph, model library and prompt profiles. It records raw input-file SHA-256s
and a separate canonical configuration digest. Graph changes, selected model or
adapter changes, profile changes and reference ordering invalidate that digest;
unrelated library additions do not invalidate an unaffected route.

It preserves disabled adapters and unresolved/conflicting pins rather than dropping
evidence. It keeps the Qwen quantized transformer, Lightning adapter, encoder and
VAE as separate components. A library `verified` flag remains a historical claim:
`installed`, `executed`, `visually_reviewed` and `accepted` remain null in this read.
Static loader activity is not tensor-execution evidence. Unknown `restart_required`
also remains null, not false. The report is not an execution ticket, signature or
atomic filesystem snapshot.

The first export successfully inspects eight existing recipes:

| Recipe | Current exact-profile observation | Qualification interpretation |
| --- | --- | --- |
| `anima-portrait` | Aesthetic 1.1 template matches | Dedicated anime creation candidate; no new art acceptance |
| `anima-v1-baseline` | Base 1.0 template matches | Retain the measured baseline; not training-only by decree |
| `anime` | Animagine 4.0 Opt template matches | Controlled SDXL baseline candidate, not universal LoRA compatibility |
| `pony`, `noob` | Legacy association only | Do not mislabel a generic association as a Pony/Noob exact compiler |
| `qwen-1ref`, `qwen-2ref`, `qwen-3ref` | Legacy association only | Separate graph configurations; the exact reference-aware profile remains missing |

Standalone Anima Turbo, Illustrious v2 Stable, Noob v-pred, native Qwen BF16,
OmniGen2 and Kontext dev are **not synthesized into registered routes** by this
report. An existing Anima Base graph containing a turbo LoRA is not automatically
the standalone Turbo checkpoint. Missing candidates remain in the portfolio's
explicit unresolved/experimental lanes until their canonical components, source
and workflow contracts exist.

## Primary-source validation and corrections

The [dated ledger](source-review-2026-09-21.json) is an audit attachment, not an
executable source/terms authority. The 18 September ledger remains historical.

The fresh AMD check corroborates the existing correction: the ROCm 10.0.0 Radeon
Ubuntu 24.04.4 row requires HWE 6.17, unlike the Instinct GA 6.8 row. The archived
Windows 7.2 matrix is a different versioned environment, not evidence that every
10.0.0 wheel/node works in the installed 7.2.1 Studio. Neither page proves an RX
9070 XT end-to-end run. #303 must capture the entire isolated OS/driver/Python/
framework/ROCm/Comfy/node tuple and rollback plan before changing a shared driver.

The pinned Animagine card supports its tag ordering and sampling guidance. A
locally chosen scheduler must not be relabelled an upstream recommendation.
Qwen's native BF16 example does not qualify a quantized, distilled four-step
configuration or establish the maximum number of independently useful roles.

Anima's **current mutable** card contains Turbo-first audition advice and a stated
individual derivative-sale exception alongside model/output distinctions. These
are new review observations, not proof they existed at the freeze or legal
clearance for any particular workflow. Preserve the unresolved exact v1.1 source
pin and review the actual applicable licence clauses before a use decision; do
not silently replace the report's Aesthetic priority or broaden a non-commercial
label into “all derivative sales prohibited.”

## Next implementation order and acceptance

1. **Exact Qwen configuration and reference contract — #34/#144 with #21/#232.**
   Reuse the three catalog graphs, their separately reported digests and existing
   source-role handoff. Bind positive/negative and ordered source targets, the exact
   Q4/Lightning/encoder/VAE set, crop transforms and model-specific negative
   semantics. Preserve legacy saved profiles. Test wrong graph, removed/disabled
   accelerator, reordered or extra references, changed source/crop and stale setup
   revisions. A native BF16 profile must be a different configuration. This is not
   permission to relax the existing text-only handoff's source-required refusal.
2. **Attested bounded runtime comparison — #302/#303/#307.** Consume existing
   resource receipts, not this inspector's nulls. Preserve the installed baseline;
   authorize one explicit smoke cell only after actual file/node/runtime pins and
   the finite cap are recorded. Count failure, uncertain dispatch and cleanup;
   never retry an uncertain prompt by creating a replacement. A future Linux
   comparator is separately installed and measured, never a silent upgrade.
3. **Accepted-asset continuation — #313/#65/#66/#72.** Ask the existing review
   surface for canon/keeper decisions (q-30), bind one reference-preserving variation
   and one protected repair, and reuse an accepted artifact through the shipped
   Studio. Retain failures, independent visual findings, operator effort and
   protected-pixel checks. The T01–T08 tasks remain staged diagnostics, not another
   all-model release programme.

Source/provider intake #765, Civitai installer regression #794 and settings-plan
fidelity #708 were inspected as active adjacent work; they are not reimplemented
here. Recheck their live heads before modifying those seams. No GPU run, new
weights, node installation, driver change, private-image publication or owner
acceptance was performed by this continuation.
