# Reconcile with the concurrently developed Workflow Lab

Read before implementation. On 11 September 2026, while this research PR was being prepared, the local agent opened [PR #19](https://github.com/Chris0Jeky/local-asset-studio/pull/19), branch `codex/studio-workflow-lab`, commit `8c8cf8ca58cf97231a8af9f000c8b7d118189123`. Its [CURRENT_STATE.md](https://github.com/Chris0Jeky/local-asset-studio/blob/8c8cf8ca58cf97231a8af9f000c8b7d118189123/CURRENT_STATE.md) is newer local-execution evidence than the main-branch snapshot used to begin this research. These results are reported by that local agent, not re-run here.

## Reuse the work that now exists

The branch records 49 presets, model/folder readiness, pinned download controls, named setups, exact recipe import, image-to-video/3D handoffs and a GLB viewer. Seven new presets completed local generation; it includes Wan video and two TRELLIS PBR trials. Do not rebuild those screens or redownload those components for the game-asset planner. Connect the new typed briefs/reference slots/task evidence to that implementation after review and merge coordination.

TRELLIS's documented upstream Linux/NVIDIA requirements remain relevant for fresh installations, but **the actual local branch now reports Radeon execution using a small reversible CPU UV-solver patch**. The earlier general compatibility warning is not evidence that local execution is impossible. Preserve the patch, provenance and exact successful settings; compare new variants in isolation. Its generated meshes include an invented ground plane and its trimmed draft may have an open base. The next work is topology/material/mechanics/engine acceptance, not merely obtaining a mesh.

The exact Seed Hunter 1.6 graph is now preserved under that branch's `workflows/community`: recorded static inspection found 154 nodes, 24 missing node classes and three additional missing weights. This resolves the earlier research pass's missing-source blocker, but does not prove the graph executes or establish its continuation/upscale claims. Read the supplied dependency report before proposing custom-node installation.

H3 remains locally blocked after two text-encoder access-violation crashes. [Issue #18](https://github.com/Chris0Jeky/local-asset-studio/issues/18) owns this compatibility work. Do not bypass the runtime block or automatically retry those jobs. Game-asset production can continue through the working image, Wan and Blender paths.

## Licence review is a separate unresolved gate

The branch reports a Hunyuan3D 2.1 draft run. That is execution evidence, not an applicable licence. The [standard Hunyuan3D 2.1 licence](https://raw.githubusercontent.com/Tencent-Hunyuan/Hunyuan3D-2.1/main/LICENSE) excludes the UK, EU and South Korea; clause 5(c) also restricts outputs outside the territory. Confirm whether separate authorization exists before further UK use or distributing those outputs. Do not assume approval for H3 in the local notes also covers Hunyuan3D or HY-Motion. This research does not revoke or overwrite the user's recorded choices; it flags the exact model-specific terms that need resolution.

## What stays new in the game-asset PR

The three multi-reference Qwen variants extend the original one-reference graph and have not received local inference yet. Krita integration, faithful inbetweens, character training and actual engine imports remain explicitly outstanding in PR #19. The planner/evidence checker, OpenRaster helper and atlas packer address intermediate production contracts; they do not supersede the new Studio executor or pretend these outstanding steps are complete.

Compare `workflows/api/qwen-api.json` before regenerating graphs. Keep the pinned provenance and validate current installed node schemas. If the original graph changed, reconcile deliberately rather than relying on a historical filename. Both PRs are additive on the same main snapshot, but a clean textual merge does not replace the integrated tests.
