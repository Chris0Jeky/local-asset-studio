# Review package and implementation handoff

18 September 2026. Strategy, future asset planning and isolated frontend prototyping only.

## Review slices

1. **#549: adaptive experience strategy.** Eight architecture/UX/source documents plus three newly open choices in the existing HUMAN_TODO. The original 25,268 bytes of that file were preserved verbatim; no prior answer or checkbox was changed.
2. **#551: asset-production kit.** Nine files, 154 planned requests, 18 shared profiles, six worlds and neutral shared visual language. The reader prints a complete selected brief without acquiring anything.
3. **#556: behavior lab companion PR.** this final stack slice adds the standalone HTML specification, policy/browser tests, qualification notes, a combined reading index and a read-only focused CI workflow. Review it after #549 and #551.

All three are additive review work. None changes production application rendering, backend behavior, model files, ComfyUI packages, source staging, generation authority or stored drafts. The lab is not loaded by the production shell. No image, video, audio, photograph, screenshot or external font was generated or acquired.

## Start here

Read [README](README.md) for direction, [UX-SPEC](UX-SPEC.md) for the 16 workflows, [STACK-ADR](STACK-ADR.md) for the framework decision and [ROADMAP](ROADMAP.md) for independently reviewable implementation steps. Open [the lab](lab/index.html) for five representative behavioral journeys. Read [its instructions](lab/README.md) before interpreting synthetic state as product behavior.

For the next asset session, use [SESSION-HANDOFF](assets/SESSION-HANDOFF.md) and select a finite set of IDs from [catalog.csv](assets/catalog.csv). The proposed pilot is one Night Shift anchor followed by its Quiet Morning counterpart, crops/poster and essential helper states. Code components, vectors, real UI captures and media generation are different work types, not a single bulk image-generation request.

## Qualification summary

The asset reader passes 11 offline tests; its catalogue resolves all 154 requests. The inline lab policy passes 14 Node tests. The document-level browser test passes 11 groups including 36 layout/skin/viewport combinations and a separate 200% zoom check, with zero observed network requests or page errors. The same textarea and its value survive presentation changes. See [QUALIFICATION](lab/QUALIFICATION.md) and [the retained report](lab/evidence.json).

Native local-file navigation is blocked in this execution environment. The successful browser run injects the exact standalone HTML through an explicitly named test option. It is not native-file, production-origin, real-workflow, media-decoder or GPU evidence. The focused CI workflow runs native-file browser mode when a hosted runner is available; a pending CI run is not counted as passing.

The ordinary repository validator was also run on the documented retained source archive plus the new files. Archive/current-main reconciliation is in [EVIDENCE](EVIDENCE.md). No fresh full-repository unit suite is claimed. Published file hashes are compared with the tested local files before delivery.

## Next production decision

The preferred engineering route is a typed read-only presentation boundary, followed by a useful Vue/TypeScript/Vite island experiment. Normal Python startup stays offline and does not require Node/npm. This is a proposal, not an installed framework. Wider renderer ownership is conditional on preserving actual draft/source/job behavior and removing the old renderer it replaces.

The newly open owner choices are `adaptive-frontend-build`, `adaptive-pilot-world` and `adaptive-remote-media` in the repository's canonical HUMAN_TODO. They concern later production defaults; they do not block reviewing the strategy already requested. Existing q-7 and #539 real-machine/owner UX acceptance remain open. Do not infer acceptance from a screenshot, a model output, an empty blocker list or these prototype tests.
