diff --git a/.github/workflows/workshop-ui.yml b/.github/workflows/workshop-ui.yml
index 5ff7d124451ba94327f99c7166bdd7b11ff9228a..ac0310d95fc6188024d3a26280a16b7b059f14ce 100644
--- a/.github/workflows/workshop-ui.yml
+++ b/.github/workflows/workshop-ui.yml
@@ -56,0 +57,4 @@ jobs:
+      - name: Dashboard and library renderer regressions
+        run: python tests/workshop_daily_journeys.py
+      - name: Overview and library journeys on native HTTP
+        run: python tests/workshop_daily_application.py --output .runtime/workshop-daily
@@ -74,0 +79 @@ jobs:
+            .runtime/workshop-daily/
diff --git a/app/static/bundle-explorer.js b/app/static/bundle-explorer.js
index b360628636c835a159a43e219af24700b7526e9e..92109a8f27a95e77d75e11ca59335ec34aac13b1 100644
--- a/app/static/bundle-explorer.js
+++ b/app/static/bundle-explorer.js
@@ -134 +134 @@
-    dialog.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter'){e.preventDefault();e.stopPropagation();}});
+    dialog.addEventListener('keydown',e=>{if(e.key==='Escape'){e.preventDefault();e.stopPropagation();dialog.close();}else if((e.ctrlKey||e.metaKey)&&e.key==='Enter'){e.preventDefault();e.stopPropagation();}});
diff --git a/app/static/studio-workbench.js b/app/static/studio-workbench.js
index 398599763b17c0e1ca9d8a8301f467fcca1d9a79..1264efeb82afc8178fdba148604549d9ddcbf184 100644
--- a/app/static/studio-workbench.js
+++ b/app/static/studio-workbench.js
@@ -638 +638,3 @@
-    q('#uxPullAsset').disabled=!takesSource()||pickerBusy||pickerLoading;
+    // The modal already blocks duplicate interaction. Keep its opener focusable for native return.
+    q('#uxPullAsset').disabled=!takesSource();
+    q('#uxPullAsset').setAttribute('aria-busy',String(pickerBusy||pickerLoading));
diff --git a/docs/workshop/UX-DAILY-JOURNEYS-2026-09-21.md b/docs/workshop/UX-DAILY-JOURNEYS-2026-09-21.md
index f50b64e1f7f3283cbea38f269ade062b432baa32..84893639dc3cb496a4a99e23ef52f5745b1338a5 100644
--- a/docs/workshop/UX-DAILY-JOURNEYS-2026-09-21.md
+++ b/docs/workshop/UX-DAILY-JOURNEYS-2026-09-21.md
@@ -25,6 +25,14 @@ Child of #793, itself a child of #790. Tracker: #772.
-`python tests/workshop_daily_journeys.py` runs exact production scripts or explicit
-legacy-function seams over synthetic native Chromium DOMs. Six new methods and
-six inherited entry-point methods pass. The new cases were red before these
-changes (missing launchers/inspection/guidance, delayed dialog and ambiguous read
-outcome), then green. Cancellation/late replies, cached failure, escaped record
-text, native focus return and both viewport widths are covered.
+`python tests/workshop_daily_journeys.py` runs production scripts or explicit
+legacy-function seams over synthetic native Chromium DOMs. Eight daily methods
+and six inherited entry-point methods pass on the integrated source. Reads and
+selected-recipe changes are deferred explicitly to test cancellation, replacement,
+duplicate-open suppression and preservation of newer keyboard focus.
+
+The retained daily branch initially passed twelve methods. Continuation added
+nonempty bundle search and cancellation-focus assertions: both viewport variants
+left the bundle dialog open after Escape, and closing a loading library picker
+lost its opener. All three assertions failed before the correction and now pass.
+The bundle handles Escape before the browser's search-field clear action. The
+library opener stays focusable while the native modal supplies interaction
+isolation; it exposes busy state without disabling native focus return. Opening
+an already-open picker cannot issue another read.
@@ -34,5 +42,25 @@ runs the full Studio at 1440px and 390px against the existing synthetic HTTP fix
-It checks the same entry points, failure-record identity, preserved drafts,
-unsupported-source explanation, library loading/failure, actual import navigation,
-page errors and zero writes except time estimates. It records screenshots and
-checks.json in the normal Workshop UI artifact. It must run in hosted CI here:
-local Chromium blocks loopback navigation, which was not bypassed.
+It checks launcher/record identity, preserved drafts, unsupported-source
+explanation, library loading/failure, actual import navigation, page errors and
+zero writes except time estimates. It records screenshots and checks.json in the
+Workshop UI artifact. Local Chromium refuses loopback navigation with
+`ERR_BLOCKED_BY_ADMINISTRATOR`; no bypass or local HTTP-browser pass is claimed.
+Hosted final-head results are recorded in the PR rather than inferred from the
+renderer fixture. The fixture's bundle catalogue is empty; card exploration and
+apply safeguards remain covered separately by the existing bundle browser suite.
+
+## Reconciliation
+
+The uploaded archive and readonly GitHub snapshot were reconstructed by exact
+Git blob, mode, commit and tree identity. The original daily tree
+`db046d436b36eb7b4d4c5618f8ae1e347d647dba` was preserved, then reconciled with the
+updated #790/#793 stack over main `7b504b902efbb48a0dc1def6a54cedba96b762d9`.
+GitHub's merge candidate and the local three-way integration independently
+produced `424698c203879258a79c65ec2e65241a8ef81e48` before the focus corrections.
+Newer main's model-intel and local-media fallback changes are preserved.
+
+#790 at `c8f90f12b7009d82802844d9344992dcef9b4b09` and #793 at
+`57d3ed313e7d3fc3bb06b295fb0b5e21a261c383` each passed all eleven hosted workflows.
+The earlier #790 Windows mixed-batch timeout and I2V hash-field error are retained
+in its history, not hidden by relaxing assertions or replacing tests. The
+integrated head includes main's independent Windows descriptor-identity fix.
+The separate lifetime-observability programme is not changed by this UX stack.
diff --git a/tests/workshop_daily_application.py b/tests/workshop_daily_application.py
index 4719ae231ce6fa78640f6cf6acef2089b291ac3a..dd40e2d2a591d18f3933bfd90b8f8e3923b774d7 100644
--- a/tests/workshop_daily_application.py
+++ b/tests/workshop_daily_application.py
@@ -40,0 +41 @@ def main():
+                    page.fill('#bundleSearch', 'ink')
@@ -67,0 +69 @@ def main():
+                    assert page.locator('#uxPullAsset').evaluate('(n)=>n===document.activeElement')
diff --git a/tests/workshop_daily_journeys.py b/tests/workshop_daily_journeys.py
index 198169d5a5d0d11378156150731a6784e444aa66..c037a75e7ea482e878ca1a3eb9f123df469eb408 100644
--- a/tests/workshop_daily_journeys.py
+++ b/tests/workshop_daily_journeys.py
@@ -34,0 +35 @@ class DailyJourneys(entry.EntryPoints):
+                self.page.fill('#bundleSearch', 'ink')
@@ -36 +37 @@ class DailyJourneys(entry.EntryPoints):
-                self.page.wait_for_function('!document.querySelector("#bundleExplorer").open')
+                self.assertFalse(self.page.locator('#bundleExplorer').evaluate('(n)=>n.open'))
@@ -76,0 +78 @@ window.pending=[];function refreshAssets(){return new Promise(resolve=>pending.p
+        self.assertTrue(self.page.locator('#uxPullAsset').evaluate('(n)=>n===document.activeElement'))
@@ -85,0 +88,21 @@ window.pending=[];function refreshAssets(){return new Promise(resolve=>pending.p
+    def test_library_loading_keeps_one_read_and_respects_newer_focus(self):
+        self.load_picker()
+        self.page.click('#uxPullAsset')
+        self.page.locator('#uxPullAsset').evaluate('(n)=>n.click()')
+        self.assertEqual(self.page.evaluate('pending.length'), 1)
+        self.page.locator('[data-ux-close="uxSourcePicker"]').focus()
+        self.page.evaluate('pending.shift()(true)')
+        self.page.wait_for_selector('[data-ux-pull]')
+        self.assertTrue(self.page.locator('[data-ux-close="uxSourcePicker"]').evaluate('(n)=>n===document.activeElement'))
+        self.page.keyboard.press('Escape')
+        self.page.wait_for_function('!picker.open')
+        self.assertTrue(self.page.locator('#uxPullAsset').evaluate('(n)=>n===document.activeElement'))
+
+    def test_recipe_change_during_library_read_does_not_render_old_cards(self):
+        self.load_picker()
+        self.page.click('#uxPullAsset')
+        self.page.evaluate('selectionEpoch++; pending.shift()(true)')
+        self.page.wait_for_function('!picker.open')
+        self.assertEqual(self.page.locator('[data-ux-pull]').count(), 0)
+        self.assertFalse(self.page.evaluate('pickerLoading'))
+
