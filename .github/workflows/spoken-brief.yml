name: Spoken brief contracts
# Cross-platform contracts for spoken briefs and narration-policy evidence.
on:
  push:
    paths:
      - 'studio_spoken/**'
      - 'studio_prompt/http_extension.py'
      - 'app/static/spoken-brief*'
      - 'tests/spoken_brief_session.cjs'
      - 'scripts/spoken_brief*.py'
      - 'scripts/speak-handoff.ps1'
      - 'scripts/strict_json.py'
      - 'scripts/voice_profile*.py'
      - 'tests/spoken_brief_fixture.py'
      - 'tests/test_spoken_brief*.py'
      - 'tests/test_voice_profile*.py'
      - 'research/voice-profiles/**'
      - 'docs/spoken-briefs/**'
      - '.github/workflows/spoken-brief.yml'
  pull_request:
    paths:
      - 'studio_spoken/**'
      - 'studio_prompt/http_extension.py'
      - 'app/static/spoken-brief*'
      - 'tests/spoken_brief_session.cjs'
      - 'scripts/spoken_brief*.py'
      - 'scripts/speak-handoff.ps1'
      - 'scripts/strict_json.py'
      - 'scripts/voice_profile*.py'
      - 'tests/spoken_brief_fixture.py'
      - 'tests/test_spoken_brief*.py'
      - 'tests/test_voice_profile*.py'
      - 'research/voice-profiles/**'
      - 'docs/spoken-briefs/**'
      - '.github/workflows/spoken-brief.yml'
permissions:
  contents: read
jobs:
  contracts:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Voice profile contracts
        run: python -m unittest discover -s tests -p "test_voice_profile*.py" -v
      - name: Spoken brief contracts
        run: python -m unittest discover -s tests -p "test_spoken_brief*.py" -v
      - name: Spoken brief CLI contract
        run: python scripts/spoken_brief.py --help
      - name: Voice qualification CLI contract
        run: python scripts/voice_profile_qualification.py --help
      - name: Parse PowerShell wrapper
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $tokens = $null
          $errors = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path 'scripts/speak-handoff.ps1'),
            [ref]$tokens,
            [ref]$errors
          )
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }
